# ⚡ OPML Gardener 快速参考

## 🚀 一分钟快速启动

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev

# 3. 打开浏览器
# http://localhost:3000
```

---

## 📂 项目结构速查

```
OPMLGardener/
├── app/                    # 📱 应用页面
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 主页面
│   └── globals.css        # 全局样式
│
├── components/            # 🧩 React 组件
│   ├── ui/               # 基础 UI 组件
│   ├── OpmlUploader.tsx  # 文件上传
│   ├── FeedTable.tsx     # 订阅源表格
│   └── FilterSidebar.tsx # 筛选侧边栏
│
├── hooks/                # 🪝 自定义 Hooks
│   └── useOpml.ts        # 状态管理
│
├── lib/                  # 🛠️ 工具函数
│   ├── utils.ts          # 通用工具
│   └── opml-parser.ts    # OPML 处理
│
└── types/                # 📝 类型定义
    └── index.ts          # 数据模型
```

---

## 🎯 核心功能清单

### 导入导出
- [x] 拖拽上传 OPML
- [x] 导出全部订阅源
- [x] 导出选中的订阅源

### 管理编辑
- [x] 表格视图
- [x] 实时编辑标题
- [x] 修改分类
- [x] 编辑链接
- [x] 删除订阅源

### 搜索筛选
- [x] 全局搜索
- [x] 分类筛选
- [x] 实时过滤

### 批量操作
- [x] 全选/取消全选
- [x] 批量选择
- [x] 去除重复

---

## 🔧 常用命令

```bash
# 开发
npm run dev          # 启动开发服务器
npm run lint         # 代码检查

# 构建
npm run build        # 构建生产版本
npm run start        # 启动生产服务器

# 部署
vercel              # 部署到 Vercel
netlify deploy      # 部署到 Netlify
```

---

## 📦 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| Next.js | 14.2+ | React 框架 |
| TypeScript | 5.3+ | 类型系统 |
| Tailwind CSS | 3.4+ | 样式框架 |
| fast-xml-parser | 4.3+ | XML 解析 |
| lucide-react | 0.344+ | 图标库 |

---

## 🗂️ 数据流

```
导入: OPML 文件 → parseOpml() → FeedItem[]
编辑: 用户操作 → useOpml Hook → 状态更新
导出: FeedItem[] → exportOpml() → 下载 OPML
```

---

## 🎨 主要组件

### `useOpml` Hook

```typescript
const {
  feeds,              // 筛选后的订阅源
  allFeeds,           // 所有订阅源
  stats,              // 统计信息
  categories,         // 分类列表
  importOpml,         // 导入函数
  exportFeeds,        // 导出函数
  updateFeed,         // 更新订阅源
  deleteFeed,         // 删除订阅源
  removeDuplicates,   // 去重
} = useOpml();
```

### `FeedItem` 类型

```typescript
type FeedItem = {
  id: string;
  title: string;
  xmlUrl: string;
  htmlUrl?: string;
  category: string;
  isSelected: boolean;
};
```

---

## 🚀 部署选项

| 平台 | 命令 | 优势 |
|------|------|------|
| Vercel | `vercel` | 零配置，自动部署 |
| Netlify | `netlify deploy` | 免费 SSL，CDN |
| 静态导出 | `npm run build` | 任意静态托管 |
| Docker | `docker build` | 容器化部署 |

---

## 📚 文档索引

- **[README.md](./README.md)** - 项目概述
- **[快速开始.md](./快速开始.md)** - 安装启动
- **[使用指南.md](./使用指南.md)** - 详细使用说明
- **[开发文档.md](./开发文档.md)** - 技术架构
- **[部署指南.md](./部署指南.md)** - 部署方案
- **[CHANGELOG.md](./CHANGELOG.md)** - 版本记录

---

## 🔑 关键文件

| 文件 | 作用 |
|------|------|
| `package.json` | 依赖和脚本 |
| `tsconfig.json` | TypeScript 配置 |
| `tailwind.config.ts` | Tailwind 配置 |
| `next.config.js` | Next.js 配置 |
| `example.opml` | 示例测试文件 |

---

## 💡 快速技巧

### 添加新组件
```bash
# 在 components/ 目录创建文件
# 使用 TypeScript 和 React Hooks
```

### 修改样式
```tsx
// 使用 Tailwind classes
<div className="flex items-center gap-4 p-6">
  {/* ... */}
</div>
```

### 添加新功能
```typescript
// 1. 在 types/ 定义类型
// 2. 在 lib/ 实现逻辑
// 3. 在 hooks/ 集成状态
// 4. 在 components/ 创建 UI
```

---

## ⚠️ 注意事项

- ✅ 所有数据在客户端处理
- ✅ 不需要后端服务器
- ✅ 完全离线可用
- ⚠️ 浏览器需支持 File API
- ⚠️ 大量订阅源（>10K）可能影响性能

---

## 🎯 下一步

1. ✅ 安装依赖：`npm install`
2. ✅ 启动项目：`npm run dev`
3. ✅ 上传测试：使用 `example.opml`
4. ✅ 体验功能：编辑、筛选、导出
5. ✅ 部署上线：选择部署平台

---

**保存此文件以便快速查阅！** 📌

